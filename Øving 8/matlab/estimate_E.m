function E = estimate_E(B1, B2)
    % Bi: Array of size 3 x n containing back-projection vectors in image i.
    n = size(B1, 2);
    A = zeros([n, 9]);
    E = eye(3); % Placeholder, replace with your implementation
end
